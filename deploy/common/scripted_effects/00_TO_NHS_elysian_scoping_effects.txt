#	Example:
# 
#	example_effect = {
#		treasury = 100
#		add_adm_power = 50
#	}
#
#
#	In a script file:
#
#	effect = {
#		example_effect = yes
#	}
#
nhs_trigger_event_for_existing_elysian_tag = {
	if = {
		limit = {
			exists = NHS
		}
		NHS = {
			country_event = { 
				id = $EVENT_ID$
				[[DAYS]
				days = $DAYS$
				]
				[[RANDOM]
				random = $RANDOM$
				]
				[[TOOLTIP]
				tooltip = $TOOLTIP$
				]
			}
		}
	}
	else_if = {
		limit = {
			exists = ELY
		}
		ELY = {
			country_event = { 
				id = $EVENT_ID$
				[[DAYS]
				days = $DAYS$
				]
				[[RANDOM]
				random = $RANDOM$
				]
				[[TOOLTIP]
				tooltip = $TOOLTIP$
				]
			}
		}
	}
	else_if = {
		limit = {
			exists = ELR
		}
		ELR = {
			country_event = { 
				id = $EVENT_ID$
				[[DAYS]
				days = $DAYS$
				]
				[[RANDOM]
				random = $RANDOM$
				]
				[[TOOLTIP]
				tooltip = $TOOLTIP$
				]
			}
		}
	}
	else_if = {
		limit = {
			exists = ERR
		}
		ERR = {
			country_event = { 
				id = $EVENT_ID$
				[[DAYS]
				days = $DAYS$
				]
				[[RANDOM]
				random = $RANDOM$
				]
				[[TOOLTIP]
				tooltip = $TOOLTIP$
				]
			}
		}
	}
	else_if = {
		limit = {
			exists = ERF
		}
		ERF = {
			country_event = { 
				id = $EVENT_ID$
				[[DAYS]
				days = $DAYS$
				]
				[[RANDOM]
				random = $RANDOM$
				]
				[[TOOLTIP]
				tooltip = $TOOLTIP$
				]
			}
		}
	}
}

nhs_scripted_effect_for_existing_elysian_tag = {
	if = {
		limit = {
			exists = NHS
		}
		NHS = {
			$SCRIPTED_EFFECT$ = yes
		}
	}
	else_if = {
		limit = {
			exists = ELY
		}
		ELY = {
			$SCRIPTED_EFFECT$ = yes
		}
	}
	else_if = {
		limit = {
			exists = ELR
		}
		ELR = {
			$SCRIPTED_EFFECT$ = yes
		}
	}
	else_if = {
		limit = {
			exists = ERR
		}
		ERR = {
			$SCRIPTED_EFFECT$ = yes
		}
	}
	else_if = {
		limit = {
			exists = ERF
		}
		ERF = {
			$SCRIPTED_EFFECT$ = yes
		}
	}
}

nhs_scripted_effect_with_parameter_for_existing_elysian_tag = {
	if = {
		limit = {
			exists = NHS
		}
		$SCRIPTED_EFFECT$ = { $PARAMETER_NAME$ = NHS }
	}
	else_if = {
		limit = {
			exists = ELY
		}
		$SCRIPTED_EFFECT$ = { $PARAMETER_NAME$ = ELY }
	}
	else_if = {
		limit = {
			exists = ELR
		}
		$SCRIPTED_EFFECT$ = { $PARAMETER_NAME$ = ELR }
	}
	else_if = {
		limit = {
			exists = ERR
		}
		$SCRIPTED_EFFECT$ = { $PARAMETER_NAME$ = ERR }
	}
	else_if = {
		limit = {
			exists = ERF
		}
		$SCRIPTED_EFFECT$ = { $PARAMETER_NAME$ = ERF }
	}
}

nhs_event_target_from_existing_elysian_tag = {
	hidden_effect = {
		if = {
			limit = {
				exists = NHS
			}
			NHS = {
				save_global_event_target_as = $EVENT_TARGET$
			}
		}
		else_if = {
			limit = {
				exists = ELY
			}
			ELY = {
				save_global_event_target_as = $EVENT_TARGET$
			}
		}
		else_if = {
			limit = {
				exists = ELR
			}
			ELR = {
				save_global_event_target_as = $EVENT_TARGET$
			}
		}
		else_if = {
			limit = {
				exists = ERR
			}
			ERR = {
				save_global_event_target_as = $EVENT_TARGET$
			}
		}
		else_if = {
			limit = {
				exists = ERF
			}
			ERF = {
				save_global_event_target_as = $EVENT_TARGET$
			}
		}
	}
}

nhs_reverse_add_opinion_of_existing_elysian_tag = {
	if = {
		limit = {
			exists = ELY
		}
		reverse_add_opinion = {
			who = ELY
			modifier = $OPINION_MOD$
		}
	}
	else_if = {
		limit = {
			exists = NHS
		}
		reverse_add_opinion = {
			who = NHS
			modifier = claimed_our_throne
		}
	}
	else_if = {
		limit = {
			exists = ELR
		}
		reverse_add_opinion = {
			who = ELR
			modifier = claimed_our_throne
		}
	}
	else_if = {
		limit = {
			exists = ERR
		}
		reverse_add_opinion = {
			who = ERR
			modifier = claimed_our_throne
		}
	}
	else_if = {
		limit = {
			exists = ERF
		}
		reverse_add_opinion = {
			who = ERF
			modifier = claimed_our_throne
		}
	}
}

nhs_add_existing_elysian_tag_as_historical_rival = {
	if = {
		limit = {
			exists = ELY
		}
		add_historical_rival = ELY
		hidden_effect = {
			add_historical_rival = NHS
			add_historical_rival = ELR
			add_historical_rival = ERR
			add_historical_rival = ERF
		}
	}
	else_if = {
		limit = {
			exists = NHS
		}
		add_historical_rival = NHS
		hidden_effect = {
			add_historical_rival = ELY
			add_historical_rival = ELR
			add_historical_rival = ERR
			add_historical_rival = ERF
		}
	}
	else_if = {
		limit = {
			exists = ELR
		}
		add_historical_rival = ELR
		hidden_effect = {
			add_historical_rival = ELY
			add_historical_rival = NHS
			add_historical_rival = ERR
			add_historical_rival = ERF
		}
	}
	else_if = {
		limit = {
			exists = ERR
		}
		add_historical_rival = ERR
		hidden_effect = {
			add_historical_rival = ELY
			add_historical_rival = NHS
			add_historical_rival = ELR
			add_historical_rival = ERF
		}
	}
	else_if = {
		limit = {
			exists = ERF
		}
		add_historical_rival = ERF
		hidden_effect = {
			add_historical_rival = ELY
			add_historical_rival = NHS
			add_historical_rival = ELR
			add_historical_rival = ERR
		}
	}
}

nhs_msg_sever_ties_with_elysia_effect = {
	if = {
		limit = {
			exists = NHS
		}
		reverse_add_opinion = {
			who = NHS
			modifier = msg_severed_ties_opinion
		}
		if = {
			limit = {
				marriage_with = NHS
			}
			break_marriage = NHS
			add_stability = 1
		}
		add_historical_rival = NHS
		hidden_effect = {
			add_historical_rival = ELY
			add_historical_rival = ELR
			add_historical_rival = ERR
			add_historical_rival = ERF
		}
		NHS = {
			country_event = { id = msg_mission.5 }
		}
	}
	else_if = {
		limit = {
			exists = ELY
		}
		reverse_add_opinion = {
			who = ELY
			modifier = msg_severed_ties_opinion
		}
		if = {
			limit = {
				marriage_with = ELY
			}
			break_marriage = ELY
			add_stability = 1
		}
		add_historical_rival = ELY
		hidden_effect = {
			add_historical_rival = NHS
			add_historical_rival = ELR
			add_historical_rival = ERR
			add_historical_rival = ERF
		}
		ELY = {
			country_event = { id = msg_mission.5 }
		}
	}
	else_if = {
		limit = {
			exists = ELR
		}
		reverse_add_opinion = {
			who = ELR
			modifier = msg_severed_ties_opinion
		}
		if = {
			limit = {
				marriage_with = ELR
			}
			break_marriage = ELR
			add_stability = 1
		}
		add_historical_rival = ELR
		hidden_effect = {
			add_historical_rival = NHS
			add_historical_rival = ELY
			add_historical_rival = ERR
			add_historical_rival = ERF
		}
		ELR = {
			country_event = { id = msg_mission.5 }
		}
	}
	else_if = {
		limit = {
			exists = ERR
		}
		reverse_add_opinion = {
			who = ERR
			modifier = msg_severed_ties_opinion
		}
		if = {
			limit = {
				marriage_with = ERR
			}
			break_marriage = ERR
			add_stability = 1
		}
		add_historical_rival = ERR
		hidden_effect = {
			add_historical_rival = NHS
			add_historical_rival = ELY
			add_historical_rival = ELR
			add_historical_rival = ERF
		}
		ERR = {
			country_event = { id = msg_mission.5 }
		}
	}
	else_if = {
		limit = {
			exists = ERF
		}
		reverse_add_opinion = {
			who = ERF
			modifier = msg_severed_ties_opinion
		}
		if = {
			limit = {
				marriage_with = ERF
			}
			break_marriage = ERF
			add_stability = 1
		}
		add_historical_rival = ERF
		hidden_effect = {
			add_historical_rival = NHS
			add_historical_rival = ELY
			add_historical_rival = ELR
			add_historical_rival = ERR
		}
		ERF = {
			country_event = { id = msg_mission.5 }
		}
	}
}

nhs_msg_incite_barbaroi_rebellion_effect = {
	if = {
		limit = {
			exists = ELY
		}
		ELY = {
			add_spy_network_from = {
				who = ROOT
				value = -90
			}
			every_owned_province = {
				limit = {
					OR = {
						culture = elysameric
						native_culture = yes
					}
				}
				set_province_flag = msg_prepped_for_barbaroi_rise_temp
			}
		}
	}
	else_if = {
		limit = {
			exists = NHS
		}
		NHS = {
			add_spy_network_from = {
				who = ROOT
				value = -90
			}
			every_owned_province = {
				limit = {
					OR = {
						culture = elysameric
						native_culture = yes
					}
				}
				set_province_flag = msg_prepped_for_barbaroi_rise_temp
			}
		}
	}
	else_if = {
		limit = {
			exists = ELR
		}
		ELR = {
			add_spy_network_from = {
				who = ROOT
				value = -90
			}
			every_owned_province = {
				limit = {
					OR = {
						culture = elysameric
						native_culture = yes
					}
				}
				set_province_flag = msg_prepped_for_barbaroi_rise_temp
			}
		}
	}
	else_if = {
		limit = {
			exists = ERR
		}
		ERR = {
			add_spy_network_from = {
				who = ROOT
				value = -90
			}
			every_owned_province = {
				limit = {
					OR = {
						culture = elysameric
						native_culture = yes
					}
				}
				set_province_flag = msg_prepped_for_barbaroi_rise_temp
			}
		}
	}
	else_if = {
		limit = {
			exists = ERF
		}
		ERF = {
			add_spy_network_from = {
				who = ROOT
				value = -90
			}
			every_owned_province = {
				limit = {
					OR = {
						culture = elysameric
						native_culture = yes
					}
				}
				set_province_flag = msg_prepped_for_barbaroi_rise_temp
			}
		}
	}
}